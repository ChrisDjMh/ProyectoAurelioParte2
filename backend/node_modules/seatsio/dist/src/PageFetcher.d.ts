import { Axios } from 'axios';
import { Page } from './Page';
import { PaginatedJson } from './AsyncIterator';
export declare class PageFetcher<T, Y> {
    client: Axios;
    pageCreator: (data: PaginatedJson<Y>) => Page<T>;
    url: string;
    constructor(url: string, client: Axios, pageCreator: (data: PaginatedJson<Y>) => Page<T>);
    fetchAfter(afterId: number | null, queryParameters: object | null, pageSize: number | null): Promise<Page<T>>;
    fetchBefore(beforeId: number, queryParameters: object | null, pageSize: number | null): Promise<Page<T>>;
    fetch(queryParameters: object | null, pageSize: number | null): Promise<Page<T>>;
}
