export declare class TableBookingConfig {
    mode: string;
    tables?: {
        [key: string]: string;
    };
    constructor(mode: string, tables?: {
        [key: string]: string;
    });
    static inherit(): TableBookingConfig;
    static allByTable(): TableBookingConfig;
    static allBySeat(): TableBookingConfig;
    static custom(tables: {
        [key: string]: string;
    }): TableBookingConfig;
}
