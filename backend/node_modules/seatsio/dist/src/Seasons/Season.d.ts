import { Event } from '../Events/Event';
import { Dict } from '../Dict';
export type SeasonJson = Dict<any>;
export declare class Season extends Event {
    events?: Event[];
    partialSeasonKeys: string[];
    constructor(season: SeasonJson);
    isSeason(): boolean;
}
