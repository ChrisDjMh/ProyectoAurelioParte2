import { HoldToken } from './HoldToken';
import { Axios } from 'axios';
export declare class HoldTokens {
    client: Axios;
    constructor(client: Axios);
    create(expiresInMinutes?: number | null): Promise<HoldToken>;
    expiresInMinutes(holdToken: string, minutes: number): Promise<HoldToken>;
    retrieve(holdToken: string): Promise<HoldToken>;
}
